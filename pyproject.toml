[build-system]
requires = ["setuptools >= 77.0.3"]

[project]
dynamic = ["version"]
name = "tsuutina_phrase_translation"
requires-python = ">=3.8"
authors = [
	{name = "Christopher Cox"},
]

[tool.setuptools.packages.find]
include = ["tsuutina_phrase_translation"]